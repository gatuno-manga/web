import { ChapterLabelPipe } from './chapter-label-pipe';

describe('ChapterLabelPipe', () => {
	it('create an instance', () => {
		const pipe = new ChapterLabelPipe();
		expect(pipe).toBeTruthy();
	});
});
