import { CommentInitialsPipe } from './comment-initials-pipe';

describe('CommentInitialsPipe', () => {
	it('create an instance', () => {
		const pipe = new CommentInitialsPipe();
		expect(pipe).toBeTruthy();
	});
});
